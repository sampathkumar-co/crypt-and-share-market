# v1.1 implementation note

The committed overlay reproduces the v1.0 raw simple-trend arm exactly before applying any factor-risk controls. This note exists only to make the child-branch implementation boundary explicit; it does not alter the frozen v1.1 protocol or acceptance thresholds.
